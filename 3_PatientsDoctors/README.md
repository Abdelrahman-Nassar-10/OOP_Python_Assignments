# HW3 — Patients / Doctors Model

Inheritance with a common base `Person(name, age)` and two subclasses:
- `Patient(medical_history)`
- `Doctor(specialty)`

A helper function prints `person.get_details()` for both types
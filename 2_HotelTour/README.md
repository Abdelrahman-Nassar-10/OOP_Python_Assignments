# HW2 — Hotel Tour

- `TravelPackage` (abstract): declares `calculate_total_cost()` and `display_package_cost()`.
- `HotelPackage`: `nights`, `cost_per_night`.
- `TourPackage`: `days`, `cost_per_day` with a 10% discount applied in display.


